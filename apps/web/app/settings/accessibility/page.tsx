import { AccessibilitySettingsPage } from "@/components/settings/accessibility-settings-page"

export const metadata = { title: "Accessibility — VortexChat" }

export default function AccessibilitySettings() {
  return <AccessibilitySettingsPage />
}
