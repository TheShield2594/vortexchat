# Supabase Cloud — get from app.supabase.com → your project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL — your Vercel deployment URL
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Voice channels use Supabase Realtime (Broadcast + Presence) — no separate signal server needed

# TURN server — required for users behind strict NAT / corporate firewalls (~20% of connections)
# Deploy coturn (https://github.com/coturn/coturn) and fill these in.
# Without TURN, peer connections will silently fail for those users.
NEXT_PUBLIC_TURN_URL=turn:your-turn-server:3478
NEXT_PUBLIC_TURN_USERNAME=your-turn-username
NEXT_PUBLIC_TURN_CREDENTIAL=your-turn-credential
# Optional: TLS TURN for networks that block UDP
NEXT_PUBLIC_TURNS_URL=turns:your-turn-server:5349

# Web Push (push notifications)
# Generate once: npx web-push generate-vapid-keys
# NEXT_PUBLIC_VAPID_PUBLIC_KEY is exposed to the browser (safe)
# VAPID_PRIVATE_KEY must be kept secret (server-side only)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
# Set to mailto:admin@yourdomain.com or https://yourdomain.com
VAPID_SUBJECT=mailto:admin@example.com
