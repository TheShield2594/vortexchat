# Supabase — get these from your Supabase Studio → Settings → API
NEXT_PUBLIC_SUPABASE_URL=http://your-coolify-domain:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Signaling server (WebSocket URL for voice)
NEXT_PUBLIC_SIGNAL_URL=ws://your-coolify-domain:3001

# TURN server — required for users behind strict NAT / corporate firewalls
# Deploy coturn (https://github.com/coturn/coturn) and fill these in.
# Without TURN, ~20% of voice connections will silently fail.
NEXT_PUBLIC_TURN_URL=turn:your-turn-server:3478
NEXT_PUBLIC_TURN_USERNAME=your-turn-username
NEXT_PUBLIC_TURN_CREDENTIAL=your-turn-credential
# Optional: TLS TURN for networks that block UDP
NEXT_PUBLIC_TURNS_URL=turns:your-turn-server:5349

# Optional: App URL (used for redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000
